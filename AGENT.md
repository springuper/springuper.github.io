# AGENT.md

给在这个仓库里协作的 AI 助手的约定。**第一节「文风」是重点**——它记录的是一组量化基线，改动文章前后都应该跑一遍核对。

---

## 一、文风（最重要）

作者的文风基线来自他自己已发布的文章（`blog/source/_posts/` 下的 `management-lever` / `just-culture` / `hire-slow` / `fire-fast` / `recruitment-driven-organization` 等）。**这些数字是实测出来的，不是印象。**

### 1.1 硬指标

| 项 | 作者旧文基线 | 说明 |
|---|---|---|
| 破折号 `——` | **0**（五篇合计 0 处） | 他写作**从不使用破折号**。需要转折/引申时用冒号、逗号、句号 |
| 加粗 `**` | **约 1.5 处/千字** | 只用于"心得句"；五篇合计仅 18 处 |
| 「不是X而是Y」 | **约 0.6 处/千字** | 保留论点级对举，其余散开 |
| 句首「而」 | **0** | 删掉句首的「而」 |
| 「恰恰」 | **0** | 整篇至多保留一处成语「恰恰相反」 |
| 口语虚词 | **约 2.3 处/千字** | `其实 / 倒是 / 罢了 / 不妨 / 的确 / 权当 / 老实说 / 坦白 / 大概`——**这类词是他的声音，不能清零** |

### 1.2 具体规矩

**不要用破折号。** 改成冒号（引出解释）、句号（另起一句）或逗号。例：

- ❌ `说得再直接一点——**向内归因能解决更多问题。**`
- ✅ `说得再直接一点：**向内归因能解决更多问题。**`

> 例外：**引用原文里的破折号要保留**（那是别人的字），以及出处行的 `> ——作者`。

**加粗要克制。** 只加在**「心得句」**上——那种一句话就能带走的原则。判断办法：如果一节里加粗超过 2 处，就是在拿加粗当装饰。

- ❌ 每段都有加粗；给两三个字加粗（`**结果**`、`**判断力**`）
- ✅ 每节 1–2 处，落在真正的结论句上

**保留口语虚词。** 这是他和"AI 腔"最大的区别。他不是刻板的书面语作者：

> 「不过也**权当**是兼听则明**吧**」
> 「既然今天这篇是畅想，**不妨**设想」
> 「**其实**我倒是觉得，这样的人越早暴露越说明我们做对了」
> 「这**的确**是一个好办法！」

**他的标志性写法**，可以放心用：四字格与成语（兼听则明、勠力同心、结硬寨打呆仗、财聚人散）、反问句（「那人家为啥有动力去把这个事情做好呢？！」）、加粗的结论句、第一人称自谦（「老实说」「坦白来说」「我虽然不是新兵，但也还在摸索」）。

**结尾一律是「自谦 + 邀请」，不是「漂亮的收束」。**

> ✅ 「管理方面我虽然不是新兵，但也还在摸索前进。如果你有更多的想法，欢迎约饭交流。」
> ✅ 「如果对你也还算是有所启发，那就是额外的大收益了。」
> ❌ 「至于它对你有多少效，回到你自己经历过的那些岔路口看一看，大概就有答案了。」
>
> （最后这种是 AI 爱写的"机巧收尾"——把球漂亮地踢回给读者。**他的收尾是把姿态放低、请人交流。**）

### 1.3 自检

改完文章跑一遍（阈值见上表；比值超过基线 1.5 倍就该回头看）：

```bash
python3 - <<'PY'
import re
t = open('blog/source/_posts/<你的文章>.md', encoding='utf-8').read()
b = re.sub(r'^---\n.*?\n---\n', '', t, flags=re.S)
b = re.sub(r'```.*?```', '', b, flags=re.S)
n = len(b) / 1000
print(f'破折号       {b.count("——"):>4}   (基线 0)')
print(f'加粗         {b.count("**")//2:>4}   {b.count("**")/2/n:.2f}/千字 (基线 1.50)')
print(f'不是X而是Y   {len(re.findall(r"不是[^。；]{2,26}(?:，而是|不是)", b)):>4}   (基线 ~0.6/千字)')
print(f'句首「而」   {len(re.findall(r"(?:^|。|；|：)\s*而(?!且)", b, re.M)):>4}   (基线 0)')
print(f'恰恰         {b.count("恰恰"):>4}   (基线 0)')
print(f'口语虚词     {sum(b.count(x) for x in ["其实","倒是","罢了","不妨","的确","大概","老实说","坦白","权当"]):>4}   (基线 ~2.3/千字)')
PY
```

---

## 二、内容原则

**不要编造事实，尤其是动作。** 不要写"我去看了产出曲线""我统计过"这类作者未必做过的事。观察到什么就写观察到什么。

**叙事优先，不是逐条对账。** 把作者口述的事实**写成一段话**，不要保住原句骨架只替换事实——那样出来的是 diff，不是文章。

- ❌ 「管理岗是增长本身创造出来的；在上一家公司，我赶上了行业最凶的几年；至于技术委员会，也不是我自己挤进去的」
- ✅ 「我加入现在公司的时候，国内团队刚刚成立，不过业务发展得不错。等老板们发现国内工程师质量高、性价比好，就开始逐渐扩大规模」

**让步要集中，并且收窄主张。** 不要在全篇散布"我可能错了"。让步集中在**一处**，承认完之后**把主张收窄到能守住的范围内**，而不是继续稀释。

**引用必须给出处，且每条参考文献都要在正文用到。** 删段落时记得同步清理参考文献，别留孤条。

**不要为了面面俱到牺牲鲜明。** 观点清楚 > 覆盖全面。澄清型内容（"这不是什么"）可以留；自我否定型内容（"我可能不对"）要限量。

---

## 三、博客工程

| 事项 | 约定 |
|---|---|
| 文章目录 | `blog/source/_posts/` |
| 文件名 | 英文，短横线连接，如 `inward-attribution.md` |
| 摘要分隔 | `<!--more-->` 放在开篇之后 |
| 草稿 | **必须用 `published: false`** |
| 发布 | 删掉 `published: false`，并把 `status` 改成 `publish`、`date` 改成发布日 |

### ⚠️ `status: draft` 不生效

Hexo 5 的 post processor **只判断 `published` 字段，完全不读 `status`**。实测：只写 `status: draft` 的文章**照常会被构建和发布**。

```yaml
# ✅ 这样才是真的不发布
status: draft
published: false
```

### 构建

- `npx hexo generate` → 输出到 `blog/public/`（已 gitignore），**不动 `docs/`**
- `bash scripts/build.sh` → 会写 `docs/`（GitHub Pages 部署目录），**平时不要跑**

---

## 四、图表与表格

**表格一律用 markdown 表格，绝不用 ASCII 画表格。** ASCII 画表格同时依赖等宽字体、中文恰好双宽、读者字体带制表符字形——三层里任何一层不成立就散架，而且 50 列以上在手机上必然横向滚动。本仓库 `marked` 已开 `gfm`，主题也有 `table/thead/th/td` 样式。

ASCII **只留给真正的图**：曲线走势、流程/循环、树状分支。这类图没有表格替代方案。

**画 ASCII 图时的注意点：**

- 中文是双宽、制表符是单宽——**对齐必须程序化验证，不能靠肉眼数空格**
- `①②③`、`▲▼`、`●`、`→` 属 Unicode 模糊宽度字符，可能渲染成 1 宽或 2 宽；**含这类字符的行不要加右侧边框**
- 结构字符（`│ ▼ ┌` 等）前面应当是纯 ASCII 空格，这样列位置才确定
- 宽度尽量 ≤48 列（移动端可读）

---

## 五、协作方式

- **结构性改动先说思路再动手**：改哪一节、为什么、代价是什么。作者会否掉不合适的方案。
- **一次别堆太多改动**，尤其是文风类改动——他会逐句看。
- **事实以作者为准。** 他会纠正细节（职级、时间线、因果），这是常态，不是例外。纠正时先弄清**区别在哪**，再动笔。
- **改完自查**：结构完整性（行数、代码围栏成对）、图对齐、`published: false`。
- 涉及批量文本替换时，**正则里不要用 `\s`**——它会把换行一起吃掉（本仓库出现过一次：363 行塌成 61 行）。用 `[ ]` 明确只匹配空格，并在写文件前断言行数没变。
