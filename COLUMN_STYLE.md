# 「前端之美」专栏写作准则

> 适用范围：所有前端方向的文章，包括 springuper.github.io 与「前端之美」知乎专栏。
> 本文件是**唯一真源**。写新文章前先读一遍，写完后逐条对照第八节自查。
> 最后更新：2026-09-20

## 一、总要求：五条硬指标

| 指标 | 一句话解释 | 反例（不要出现） |
|---|---|---|
| **内部硬核** | 结论必须落到机制、源码或数据上，不能停在"它很好用" | "Playwright 功能强大、生态完善" |
| **有趣** | 语言活泼、有幽默、有彩蛋 | 全篇教科书腔 |
| **有料** | 对当下前端从业者有可带走的参考价值，且每条都有来源证据 | 只有观点没有出处 |
| **引人入胜** | 会讲故事，用 demo / 动图 / ASCII 图把精华讲清楚 | 一上来就是概念定义 |
| **文风朴实** | 不端着、接地气、说人话 | "赋能""抓手""闭环""生态位" |

五条不是并列关系：**硬核是底线，有料是目的，有趣和引人入胜是手段，朴实是底色。**

## 二、硬核怎么写

1. **每个论断都要能追问到源头**。机制类结论给出源码路径或官方文档链接；数据类结论给出抓取口径与日期。
2. **区分四种证据等级，并在文中显式标注**：
   - ① 官方原文（文档 / README / 发布说明 / 仓库 commit）
   - ② 第三方统计（npm API、调查、生态站）
   - ③ 个人观点（某个工程师的博客、我的判断）
   - ④ 我自己的实测（本文最值钱的部分，必须写清版本 + 日期 + 方法）
3. **不确定就写"未验证"**。宁可在正文里承认"官方没有量化声明，下面这个数字是我自己量的"，也不要借官方之名说自己的话。
4. **主动纠偏**。遇到流传很广但不准的说法，专门列表写出来（例：某个工具的流行认知已经过时）。纠偏条目的价值往往高于正文。
5. **反方必写**。一篇只有正面论证的文章是软文。必须有一节讲"什么情况下我不选它"，并且引到有出处的批评。
6. **允许自嘲**。承认自己项目里的坏味道，比举一个虚构的反例可信得多。

## 三、有趣怎么写

- **比喻要成体系**，最好全文复用同一个（鞍具 / 乐高 / 光谱 / 接力棒），而不是每节换个花哨说法。
- **彩蛋单独成节放在结尾**，长度控制在 1～3 段，内容必须是真的：掌故、考古发现、一个可爱的细节、一个反直觉的事实。不要为了彩蛋编故事。
- **幽默来自具体**：一个真实的报错原文、一个荒谬的默认值、一个 220 倍的数字，比一句俏皮话更好笑。
- 允许口语连接词："说句公道话""顺带说一句""这里得替它辩护一句"。
- **不要**：自嘲式标题党、网络烂梗、感叹号堆砌。

## 四、有料怎么写

- 读者读完要能**带走至少三样东西**：一张能贴到团队文档里的表、一个能马上用的命令或写法、一条能改变决策的事实。
- 面向"现阶段的前端从业者"：默认读者写过 React/TS、跑过 CI、但没时间读源码。术语第一次出现必须用一句人话解释。
- 版本号、star 数、下载量、调查数据**一律标抓取日期**，并在文末写口径声明（下载量含 CI 重复安装与间接依赖之类）。
- 篇幅服务内容：3000～8000 字都正常，但每一节都必须有"带走的东西"。宁可砍节，不要注水。

## 五、引人入胜怎么写

- **开头三选一，禁止"随着……的发展"**：
  1. 一个具体场景（我今天做了件什么事）；
  2. 两段代码/两个时代的对比，同一件事不同写法；
  3. 一个反直觉的数据。
- 每节结尾给一句小结（可以是加粗的一句话），让读者随时能停下。
- 表达手段轮换使用，不要连续三节都是纯文字：**ASCII 图 / 代码块 / 真实报错 / 表格 / 一行命令**交替出现。
- ASCII 图优先用**树形或开放右边框**的画法（中文是全角字符，画右边框极易错位）。图里尽量少放中文。
- demo 优先"能跑"：真实命令 + 真实输出。图片与 GIF 在草稿里用 HTML 注释标出位置与拍摄内容，例如
  `<!-- TODO(gif): npx playwright codegen 录制 8 秒，展示 locator 自动补全 -->`
- 埋钩子：在正文中段给读者一个"往下看会有反转"的预告，但要真的兑现。

## 六、文风

- 短句为主，第二人称"你"。段落尽量不超过 5 行。
- 术语先落地再展开：先说"这个报错会把正确答案列给你"，再说 strict mode violation 是什么。
- 不用营销腔、不用咨询黑话（赋能 / 抓手 / 闭环 / 心智 / 生态位 / 颗粒度）。
- 结论句加粗，但一节最多两三处，多了等于没有重点。
- 中文标点，代码与命令一律用反引号。
- 允许"朴素但准确"胜过"漂亮但模糊"。

## 七、交付物与格式

### 目录与命名
- 文章放 `blog/source/_posts/`，文件名用英文小写 + 连字符，如 `playwright-ai-era.md`。
- 图片放 `blog/source/images/`，文件名 `文章名-序号.jpg`，正文用 `/images/xxx.jpg`。

### front matter 模板

```yaml
---
layout: post
title: "主标题：副标题"
date: 2026-09-20 20:00:00
status: draft          # 只给人看的标记，Hexo 不读它
published: false       # 唯一真正生效的字段；不加它，草稿会照常发布
tags:
  - Frontend
  - 其他标签
---
```

### 正文结构约定
1. 摘要段（首页展示）：说清"这篇文章解决什么问题、你会得到什么"，**不要剧透结论**。
2. `<!--more-->`
3. 主体：编号小节（`## 一、` `## 二、` …），每节可带 `###`。
4. 结语：用一张表把全文收束，方便回看。
5. 彩蛋（可选但强烈建议）。
6. **参考**：分类列链接（官方文档 / 源码 / 统计来源 / 延伸阅读）。
7. 文末口径声明：`> 本文数据基于 X 版本（日期），版本不同可能有出入。`

## 八、发布前自查清单

- [ ] 开头是场景 / 代码对比 / 反直觉数据，不是概念定义
- [ ] 每条事实都能指到源头，且标了证据等级
- [ ] 不确定的地方写了"未验证"，没有拿自己的数据冒充官方结论
- [ ] 有一节反方与边界，且引到了有出处的批评
- [ ] 有一节"顺带纠偏"，纠正至少一条流传很广的过时说法
- [ ] 至少有一处我自己的实测（含版本、日期、方法）
- [ ] 所有数字标了抓取日期，文末有口径声明
- [ ] 读者能带走 ≥3 样东西（表格 / 命令 / 决策依据）
- [ ] 有 ASCII 图或表格，且中文没有把图画歪
- [ ] 图片与 GIF 的位置已用注释标出，拍摄内容明确
- [ ] 彩蛋是真的，不是编的
- [ ] 参考链接可用，无死链
- [ ] 通读一遍，删掉所有营销腔词汇
